"use strict";

import {DefaultState} from "../constants/StateConstants";

const lectures = (state = DefaultState.lectures, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default lectures;
